#Implementing the naive bayes class

class Naive_Bayes:
  def __init__(step_size, name, binary):
    self.name = name
    if(binary)      #is it a binary of multiclass thing
        self.type==True
    else
        self.type==False
    

def fit(self, training_data, training_labels):
    print("Hello my name is " + self.name)


def predict(self, test_data, test_labels):
    category= 0
    print("We predict that "+self.name+"is category "+category)
    return category

print(p1.name)
print(p1.age)